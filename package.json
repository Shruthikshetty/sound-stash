{
  "name": "sound-stash",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "bun --filter backend dev",
    "deploy:backend": "bun --filter backend deploy",
    "generate-http-status-codes": "bun scripts/generate-http-status-codes.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "prettier": "^3.9.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
